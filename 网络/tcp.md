### 为什么TCP挥手是四次，而不是三次

1. 第一次客户端发出连接释放报文，并不再传输数据，
2. 这时服务端发出确认报文，然后继续传输数据，
3. 等服务端最后的数据传输完成之后，会发出释放报文，
4. 客户端发出确认报文，服务端断开连接，客户端等待2*MSL（最长报文段寿命）时间断开

==因为服务端在第一次收到释放报文时，还有数据要传输，不能发送可以释放报文，只能发送确认收到客户端释放报文的回答报文（代表我这边收到了你的信息，如果长时间不发送回答报文，客户端会认为我发的报文你没有收到，会再次发送），等数据传输完成服务端才能发送释放报文，以求释放==。