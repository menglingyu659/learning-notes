## 概述

#### 模块化（module）

通常是以**单个文件的形式**存在的功能片段，入口文件通常称之为**入口模块**或**主模块**

#### 库（libiary，简称lib）

以一个或多个模块组成的完整功能块，为开发中某一方面的问题提供完整的解决方案

#### 包（package）

包含元数据的库，这些元数据包括：名称、描述、git主页、许可证协议、作者、以来等等

### 没有包管理器时模块化存在的问题

#### 下载过程繁琐

- 进入官网或github下载
- 找到并下载相应的版本
- 拷贝到工程的目录中
- 如果遇到有同名的库，需要修改名称

#### 如果该库需要以来其他库（还会存在链式以来情况~依赖的库还依赖了其他库~），还需要按照要求先下载其他库

#### 开发环境中安装的大量的库如何在生产环境中还原，又如何区分这个库是哪个环境需要的

#### 更新一个库麻烦

#### 自己开发的库，如何在下一次的开发使用

